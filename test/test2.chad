// use std::math::*;

fn add_numbers(x, y) {
    let result = x + y;

    println("x is: ", x, ", res is: ", result);

    return result;
}

fn first() {
    first();
}

fn main() {
    // first();

    let res = add_numbers(5, 10);
    println(res);

    let res2 = add_numbers(20, 10);
    println(res2);
}
