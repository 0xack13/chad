fn add_numbers(x: int, y: int) -> int {
    let result = x + y;

    return result;
}

fn main() {
    let res: int = add_numbers(5, 10);

    println("Res is: ", res);

    let res2: int = add_numbers(20, 10);

    println("Res2 is: ", res2);
}
